# Backend API base URL.
# Local dev: leave BLANK (calls stay relative and Vite proxies /api to localhost:5001).
# Production (Vercel): set to your deployed backend, e.g. https://angel-api.onrender.com
VITE_API_URL=
